<UserControl x:Class="LorModEditor.Views.BookEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:core="clr-namespace:LorModEditor.Core;assembly=LorModEditor.Core"
             xmlns:viewModels="clr-namespace:LorModEditor.ViewModels"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:components="clr-namespace:LorModEditor.Views.Components"
             x:Name="Root"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=viewModels:BookEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#F5F5F5" Padding="10,6" BorderBrush="#DDD"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建书页" Command="{Binding CreateCommand}"
                        IsEnabled="{Binding Manager.BookRepo.HasData}"
                        Padding="12,5" Margin="0,0,10,0" FontWeight="Bold" />
                <Button Content="🗑️ 删除书页" Command="{Binding DeleteCommand}"
                        Padding="12,5" Background="#FFE6E6" BorderBrush="#FFCCCC" />
            </StackPanel>
        </Border>

        <!-- 列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top" Margin="5" Padding="5" BorderBrush="#CCC"
                     BorderThickness="1" TextChanged="SearchBox_TextChanged" ToolTip="输入 ID 或 名称 搜索..." />
            <ListBox x:Name="BookList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.BookRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="BookList_SelectionChanged"
                     BorderThickness="0,0,1,0" BorderBrush="#DDD" />
        </DockPanel>

        <!-- 编辑区 -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">

            <StackPanel x:Name="EditPanel" Margin="20" DataContext="{Binding SelectedItem}">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.7" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="核心书页编辑 (Key Page)" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333" />

                <!-- 1. 基础信息 -->
                <Expander Header="⚙️ 基础参数 (Basic)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="ID:" FontWeight="Bold" />
                                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Name:" FontWeight="Bold" />
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!-- 生成对话按钮 (依然用 CodeBehind，因为不涉及数据修改，纯工具) -->
                            <Button Grid.Column="3" Content="💬 生成对话模板" Click="GenDialog_Click"
                                    Margin="10,12,0,0" Padding="10,5" Background="#E6F7FF" BorderBrush="#1890FF" />
                        </Grid>

                        <!-- Episode, Rarity, Chapter, RangeType -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Episode (接待ID):" />
                                <TextBox Text="{Binding Episode, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Rarity:" />
                                <ComboBox ItemsSource="{x:Static core:GlobalValues.Rarities}"
                                          SelectedItem="{Binding Rarity, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Chapter (等级):" />
                                <ComboBox ItemsSource="{x:Static core:GlobalValues.BookChapters}"
                                          SelectedItem="{Binding Chapter, UpdateSourceTrigger=PropertyChanged}"
                                          ToolTipService.ShowOnDisabled="True" ToolTip="1:传闻...7:杂质" />
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Text="RangeType:" />
                                <ComboBox ItemsSource="{x:Static core:GlobalValues.RangeTypes}"
                                          SelectedItem="{Binding RangeType, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- 图标 (带预览) -->
                            <StackPanel Grid.Column="0">
                                <components:ImageSelector LabelText="Book Icon (图标):"
                                                          SelectedImageName="{Binding BookIcon, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                          ImageList="{Binding DataContext.Manager.Artworks, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          Manager="{Binding DataContext.Manager, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          PreviewWidth="100" PreviewHeight="100" />
                            </StackPanel>

                            <!-- 皮肤 (手写或默认列表) -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Character Skin (皮肤):" />
                                <!-- 这里 ItemsSource 可以绑定一个 GlobalValues.CommonSkins 列表 -->
                                <ComboBox IsEditable="True"
                                          Text="{Binding CharacterSkin, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBox.Items>
                                        <system:String>Default</system:String>
                                        <system:String>Liwei</system:String>
                                        <system:String>Roland</system:String>
                                        <system:String>Angela</system:String>
                                    </ComboBox.Items>
                                </ComboBox>
                            </StackPanel>

                            <!-- 占位 -->
                            <StackPanel Grid.Column="4" />
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- 2. 战斗数值 -->
                <Expander Header="📊 战斗数值 (Stats)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" /><ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
                            <TextBlock Text="HP:" />
                            <TextBox Text="{Binding HP, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                            <TextBlock Text="Break:" />
                            <TextBox Text="{Binding Break, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="4" Margin="0,0,0,10">
                            <TextBlock Text="Speed:" />
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding SpeedMin, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                                <TextBlock Text=" - " VerticalAlignment="Center" />
                                <TextBox Text="{Binding Speed, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Start Light:" />
                            <TextBox Text="{Binding StartPlayPoint, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock Text="Max Light:" />
                            <TextBox Text="{Binding MaxPlayPoint, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                </Expander>

                <!-- 3. 抗性表格 -->
                <Expander Header="🛡️ 抗性 (Resistances)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="1" Text="🔪 斩击" HorizontalAlignment="Center"
                                   Foreground="Gray" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="📌 穿刺" HorizontalAlignment="Center"
                                   Foreground="Gray" />
                        <TextBlock Grid.Row="0" Grid.Column="3" Text="🔨 打击" HorizontalAlignment="Center"
                                   Foreground="Gray" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="物理" VerticalAlignment="Center" FontWeight="Bold" />
                        <ComboBox Grid.Row="1" Grid.Column="1" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding SResist, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Grid.Row="1" Grid.Column="2" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding PResist, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Grid.Row="1" Grid.Column="3" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding HResist, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="混乱" VerticalAlignment="Center" FontWeight="Bold" />
                        <ComboBox Grid.Row="2" Grid.Column="1" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding SBResist, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Grid.Row="2" Grid.Column="2" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding PBResist, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Grid.Row="2" Grid.Column="3" Margin="2"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding HBResist, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </Expander>

                <!-- 4. 专属被动 (MVVM 版) -->
                <Expander Header="⚡ 核心被动 (Passives)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="选择被动:" VerticalAlignment="Center" Margin="0,0,10,0" />

                            <!-- 绑定到 ViewModel.SelectedPassiveToAdd -->
                            <ComboBox Grid.Column="1" x:Name="PassiveSelector"
                                      IsEditable="True"
                                      StaysOpenOnEdit="True"
                                      IsTextSearchEnabled="False"
                                      ItemsSource="{Binding DataContext.Manager.PassiveRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding DataContext.SelectedPassiveToAdd, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="DisplayName" Height="26" />

                            <!-- 绑定到 ViewModel.AddPassiveCommand -->
                            <Button Grid.Column="2" Content="➕ 添加"
                                    Command="{Binding DataContext.AddPassiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Padding="10,2" Margin="5,0,0,0" />
                        </Grid>

                        <ListBox ItemsSource="{Binding Passives}" Height="100" BorderBrush="#CCC" BorderThickness="1">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent" Padding="5,2">
                                        <DockPanel>
                                            <!-- 绑定 RemovePassiveCommand，参数是当前项 (LorId) -->
                                            <Button Content="❌" DockPanel.Dock="Right"
                                                    Command="{Binding DataContext.RemovePassiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="5,0" Margin="10,0,0,0" Foreground="Red" />
                                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Expander>

                <!-- 5. 专属书页 (MVVM 版) -->
                <Expander Header="🃏 专属书页 (Only Cards)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="选择卡牌:" VerticalAlignment="Center" Margin="0,0,10,0" />

                            <!-- 绑定到 ViewModel.SelectedCardToAdd -->
                            <ComboBox Grid.Column="1" x:Name="OnlyCardSelector"
                                      IsEditable="True"
                                      StaysOpenOnEdit="True"
                                      IsTextSearchEnabled="False"
                                      ItemsSource="{Binding DataContext.Manager.CardRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding DataContext.SelectedCardToAdd, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="DisplayName" Height="26" />

                            <Button Grid.Column="2" Content="➕ 绑定"
                                    Command="{Binding DataContext.AddOnlyCardCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Padding="10,2" Margin="5,0,0,0" />
                        </Grid>

                        <ListBox ItemsSource="{Binding OnlyCards}" Height="100" BorderBrush="#CCC" BorderThickness="1">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent" Padding="5,2">
                                        <DockPanel>
                                            <Button Content="❌" DockPanel.Dock="Right"
                                                    Command="{Binding DataContext.RemoveOnlyCardCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="5,0" Margin="10,0,0,0" Foreground="Red" />
                                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Expander>

                <!-- 6. 书籍故事 -->
                <Expander Header="📖 书籍故事" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD" BorderThickness="1">
                    <StackPanel Margin="10">
                        <TextBlock Text="TextList / Desc (段落):" FontWeight="Bold" Margin="0,0,0,5" />
                        <TextBox Text="{Binding BookStory, UpdateSourceTrigger=PropertyChanged}" Height="150"
                                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                 Padding="5" BorderBrush="#CCC" BorderThickness="1" />
                    </StackPanel>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>