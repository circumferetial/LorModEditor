<UserControl x:Class="LorModEditor.Views.EnemyEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodels="clr-namespace:LorModEditor.ViewModels"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=viewmodels:EnemyEditorViewModel, IsDesignTimeCreatable=False}"
             x:Name="Root"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#F5F5F5" Padding="10,6" BorderBrush="#DDD"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建敌人" Command="{Binding CreateCommand}"
                        IsEnabled="{Binding Manager.EnemyRepo.HasData}" Padding="12,5" Margin="0,0,10,0"
                        FontWeight="Bold" />
                <Button Content="🗑️ 删除敌人" Command="{Binding DeleteCommand}" Padding="12,5" Background="#FFE6E6"
                        BorderBrush="#FFCCCC" />
            </StackPanel>
        </Border>

        <!-- 列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top" Margin="5" Padding="5" BorderBrush="#CCC"
                     BorderThickness="1" TextChanged="SearchBox_TextChanged" />
            <ListBox x:Name="EnemyList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.EnemyRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="EnemyList_SelectionChanged"
                     BorderThickness="0,0,1,0" BorderBrush="#DDD" />
        </DockPanel>

        <!-- 编辑区 -->
        <ScrollViewer x:Name="ScrollViewer" Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EditPanel" Margin="20" DataContext="{Binding SelectedItem}">

                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.7" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="敌人单位编辑 (Enemy Unit)" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"
                           Foreground="#333" />

                <!-- 1. 基础信息 -->
                <Expander Header="⚙️ 基础参数 (Basic)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="ID:" FontWeight="Bold" />
                                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Name:" FontWeight="Bold" />
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="NameID (翻译ID):" Foreground="Gray" />
                                <TextBox Text="{Binding NameId, UpdateSourceTrigger=PropertyChanged}" ToolTip="默认与ID相同" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Height (身高范围):" Margin="0,0,0,5" />
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding MinHeight, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                                <TextBlock Text=" - " VerticalAlignment="Center" Margin="5,0" />
                                <TextBox Text="{Binding MaxHeight, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                            </StackPanel>
                        </StackPanel>

                        <!-- 核心书页 -->
                        <TextBlock Text="Equip Page (核心书页):" Margin="0,5,0,5" FontWeight="Bold" />
                        <DockPanel>
                            <Button DockPanel.Dock="Right" Content="清除" Click="ClearBook_Click" Padding="10,2"
                                    Margin="5,0,0,0" />
                            <ComboBox SelectedValue="{Binding BookId, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="GlobalId"
                                      DisplayMemberPath="DisplayName"
                                      ItemsSource="{Binding DataContext.Manager.BookRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </DockPanel>
                    </StackPanel>
                </Expander>

                <!-- 2. 行为与掉落 -->
                <Expander Header="🎁 行为与掉落 (Behavior &amp; Drops)" IsExpanded="True" Margin="0,0,0,15"
                          BorderBrush="#DDD" BorderThickness="1">
                    <StackPanel Margin="10">
                        <CheckBox Content="死亡时撤退 (Retreat)"
                                  IsChecked="{Binding Retreat, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                                  FontWeight="Bold" />
                        <Separator Margin="0,0,0,10" />

                        <DockPanel Margin="0,0,0,5">
                            <TextBlock Text="掉落等级 (Level):" VerticalAlignment="Center" Margin="0,0,5,0" />
                            <TextBox Text="{Binding DropLevel, UpdateSourceTrigger=PropertyChanged}" Width="50" />

                            <!-- 绑定 AddDropCommand -->
                            <Button DockPanel.Dock="Right" Content="➕ 添加书籍"
                                    Command="{Binding DataContext.AddDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Padding="10,2" />
                        </DockPanel>

                        <ItemsControl ItemsSource="{Binding Drops}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EEE" BorderThickness="1" Margin="0,2" Padding="5"
                                            Background="#FAFAFA">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" /><ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="30" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Prob:" VerticalAlignment="Center" FontSize="10"
                                                           Foreground="Gray" />
                                                <TextBox Text="{Binding Prob, UpdateSourceTrigger=PropertyChanged}"
                                                         Width="30" Margin="2,0,0,0" />
                                            </StackPanel>
                                            <ComboBox Grid.Column="1" Margin="5,0"
                                                      ItemsSource="{Binding DataContext.Manager.DropBookRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedValue="{Binding BookId, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="GlobalId"
                                                      DisplayMemberPath="DisplayName"
                                                      IsEditable="True" />

                                            <!-- 绑定 RemoveDropCommand -->
                                            <Button Grid.Column="2" Content="❌" Foreground="Red"
                                                    Command="{Binding DataContext.RemoveDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Expander>

                <!-- 3. 卡组构建 -->
                <Expander Header="🃏 卡组构建 (Deck)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <Grid Margin="10" Height="300">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- 当前卡组 (支持双击移除) -->
                        <DockPanel Grid.Column="0">
                            <TextBlock DockPanel.Dock="Top" Text="当前持有 (Deck)" FontWeight="Bold"
                                       HorizontalAlignment="Center" Margin="0,0,0,5" />
                            <ListBox x:Name="CurrentDeckList"
                                     VirtualizingStackPanel.IsVirtualizing="True"
                                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                                     ItemsSource="{Binding DeckCardIds}"
                                     BorderBrush="#CCC" BorderThickness="1"
                                     HorizontalContentAlignment="Stretch">

                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <!-- 背景透明，确保整行都能点 -->
                                        <Border Background="Transparent" Padding="5,2">
                                            <Border.InputBindings>
                                                <!-- 【2. 修改绑定方式】使用 ElementName=Root -->
                                                <MouseBinding Gesture="LeftDoubleClick"
                                                              Command="{Binding DataContext.RemoveCardFromDeckCommand, ElementName=Root}"
                                                              CommandParameter="{Binding}" />
                                            </Border.InputBindings>

                                            <TextBlock Text="{Binding}" ToolTip="双击移除" VerticalAlignment="Center" />
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="⬅️" FontSize="24" Foreground="Gray" />
                            <TextBlock Text="双击" FontSize="10" Foreground="Gray" />
                        </StackPanel>

                        <!-- 卡牌库 (支持双击添加) -->
                        <DockPanel Grid.Column="2">
                            <TextBlock DockPanel.Dock="Top" Text="卡牌库 (Library)" FontWeight="Bold"
                                       HorizontalAlignment="Center" Margin="0,0,0,5" />
                            <!-- ❌ 错误写法：同时有 DisplayMemberPath 和 ItemTemplate -->
                            <!-- <ListBox x:Name="LibraryCardList" DisplayMemberPath="DisplayName" ...> -->

                            <!-- ✅ 正确写法：删掉 DisplayMemberPath -->
                            <ListBox x:Name="LibraryCardList"
                                     ItemsSource="{Binding DataContext.Manager.CardRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     BorderBrush="#CCC" BorderThickness="1"
                                     HorizontalContentAlignment="Stretch">

                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <!-- 别忘了在这里手动绑定显示文字 -->
                                        <Border Background="Transparent" Padding="5,2">
                                            <Border.InputBindings>
                                                <MouseBinding Gesture="LeftDoubleClick"
                                                              Command="{Binding DataContext.AddCardToDeckCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              CommandParameter="{Binding}" />
                                            </Border.InputBindings>
                                            <!-- 手动显示名字 -->
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" />
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Grid>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>