<UserControl x:Class="LorModEditor.Views.KeywordEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:wrappers="clr-namespace:LorModEditor.Core.Wrappers;assembly=LorModEditor.Core"
             xmlns:viewModels="clr-namespace:LorModEditor.ViewModels"


             mc:Ignorable="d"

             d:DataContext="{d:DesignInstance Type=viewModels:KeywordEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#F5F5F5" Padding="10" BorderBrush="#DDD"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建词条" Command="{Binding CreateCommand}" Padding="10,5" Margin="0,0,10,0"
                        FontWeight="Bold"
                        IsEnabled="{Binding Manager.KeywordRepo.HasLoc}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Manager.KeywordRepo.HasLoc}" Value="False">
                                    <Setter Property="ToolTip" Value="[不可用] 未找到可写入的翻译文件" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="🗑️ 删除词条" Command="{Binding DeleteCommand}" Padding="10,5" Background="#FFE6E6"
                        BorderBrush="#FFCCCC" Margin="0,0,20,0" />
            </StackPanel>
        </Border>

        <!-- 左侧列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top"
                     TextChanged="SearchBox_TextChanged"
                     Margin="5" Padding="5" BorderBrush="#CCC" BorderThickness="1"
                     ToolTip="输入 ID, 名字 或 描述 进行搜索..." />

            <!-- 改名为 KeywordListBox -->
            <ListBox x:Name="KeywordListBox"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.KeywordRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="KeywordListBox_SelectionChanged"
                     BorderThickness="0,0,1,0" BorderBrush="#DDDDDD" />
        </DockPanel>

        <!-- 右侧编辑区 -->
        <StackPanel x:Name="EditPanel" Grid.Row="1" Grid.Column="1" Margin="20" Visibility="Hidden"
                    d:DataContext="{d:DesignInstance Type=wrappers:UnifiedKeyword}">

            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                            <Setter Property="Opacity" Value="0.7" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <TextBlock Text="关键字/效果文本编辑 (BattleEffectText)" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"
                       Foreground="#333" />

            <TextBlock Text="ID:" FontWeight="Bold" />
            <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="2" />

            <TextBlock Text="Name (显示名称):" FontWeight="Bold" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="2" />

            <TextBlock Text="Desc (描述):" FontWeight="Bold" />
            <TextBlock Text="* 支持多行，支持 {0} 占位符" FontSize="10" Foreground="Gray" Margin="0,0,0,2" />

            <TextBox Text="{Binding Desc, UpdateSourceTrigger=PropertyChanged}"
                     Height="200"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="5"
                     BorderBrush="#CCC" BorderThickness="1" />
        </StackPanel>
    </Grid>
</UserControl>