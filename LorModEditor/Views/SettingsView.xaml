<UserControl x:Class="LorModEditor.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodels="clr-namespace:LorModEditor.ViewModels"

             xmlns:sys="clr-namespace:System;assembly=mscorlib"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Background="White">
        <StackPanel Margin="30" MaxWidth="700" HorizontalAlignment="Left" VerticalAlignment="Top">

            <TextBlock Text="⚙️ 编辑器设置 (Settings)" FontSize="26" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333" />
            <Separator Margin="0,0,0,20" />

            <!-- 1. 常规设置 -->
            <GroupBox Header="🌍 常规 (General)" Margin="0,0,0,20" Padding="15">
                <StackPanel>
                    <TextBlock Text="Target Language (目标语言):" FontWeight="Bold" Margin="0,0,0,5" />
                    <TextBlock Text="* 决定读取 Localize 下的哪个文件夹 (cn/en/kr...)" FontSize="11" Foreground="Gray"
                               Margin="0,0,0,5" />

                    <!-- 可编辑下拉框，支持自定义语言代码 -->
                    <ComboBox IsEditable="True"
                              Text="{Binding Config.Language, UpdateSourceTrigger=PropertyChanged}"
                              Width="150" HorizontalAlignment="Left" Height="28">
                        <ComboBox.Items>
                            <sys:String>cn</sys:String>
                            <sys:String>en</sys:String>
                            <sys:String>kr</sys:String>
                            <sys:String>jp</sys:String>
                            <sys:String>tr</sys:String>
                        </ComboBox.Items>
                    </ComboBox>
                </StackPanel>
            </GroupBox>

            <!-- 2. 原版数据设置 -->
            <GroupBox Header="📦 原版数据 (Vanilla Data)" Margin="0,0,0,20" Padding="15">
                <StackPanel>
                    <!-- 开关 -->
                    <CheckBox Content="加载原版游戏数据 (Load Vanilla Data)"
                              IsChecked="{Binding Config.LoadVanillaData}"
                              FontSize="14" Margin="0,0,0,10"
                              ToolTip="关闭后可加快加载速度，但在选择器中将无法看到原版卡牌和被动" />

                    <!-- 路径选择 -->
                    <TextBlock Text="BaseMod 路径 (LibraryOfRuina_Data\Managed\BaseMod):" Margin="0,10,0,5"
                               FontWeight="Bold" />
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="📂 浏览..."
                                Command="{Binding BrowseCommand}"
                                Padding="15,5" Margin="10,0,0,0" />

                        <TextBox Text="{Binding Config.BaseModPath, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="5" VerticalContentAlignment="Center" Background="#F8F8F8" />
                    </DockPanel>
                </StackPanel>
            </GroupBox>

            <!-- 3. 操作区 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <!-- 保存 -->
                <Button Command="{Binding SaveCommand}"
                        Content="💾 保存设置"
                        Padding="20,10" Margin="0,0,15,0"
                        FontSize="14" FontWeight="Bold"
                        Background="#4CAF50" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4" />
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- 重载 -->
                <Button Command="{Binding ReloadCommand}"
                        Content="🔄 重新加载项目"
                        Padding="20,10"
                        FontSize="14"
                        ToolTip="应用新的语言或路径设置" />
            </StackPanel>

            <TextBlock Text="* 更改语言或路径后，请点击“重新加载项目”以生效。"
                       Foreground="Gray" Margin="0,10,0,0" HorizontalAlignment="Right" />

        </StackPanel>
    </Grid>
</UserControl>