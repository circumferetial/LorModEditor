<UserControl x:Class="LorModEditor.Views.StageEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:core="clr-namespace:LorModEditor.Core;assembly=LorModEditor.Core"
             xmlns:wrappers="clr-namespace:LorModEditor.Core.Wrappers;assembly=LorModEditor.Core"
             xmlns:viewmodels="clr-namespace:LorModEditor.ViewModels"
             xmlns:components="clr-namespace:LorModEditor.Views.Components"

             mc:Ignorable="d"

             prism:ViewModelLocator.AutoWireViewModel="True"

             d:DataContext="{d:DesignInstance Type=viewmodels:StageEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#F5F5F5" Padding="10,6" BorderBrush="#DDD"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建关卡" Command="{Binding CreateCommand}" Padding="12,5" Margin="0,0,10,0"
                        FontWeight="Bold" />
                <Button Content="🗑️ 删除关卡" Command="{Binding DeleteCommand}" Padding="12,5" Background="#FFE6E6"
                        BorderBrush="#FFCCCC" />
            </StackPanel>
        </Border>

        <!-- 列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top"
                     Margin="5" Padding="5" BorderBrush="#CCC" BorderThickness="1"
                     TextChanged="SearchBox_TextChanged" ToolTip="输入 ID 或 名称 搜索..." />

            <ListBox x:Name="StageList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.StageRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="StageList_SelectionChanged"
                     BorderThickness="0,0,1,0" BorderBrush="#DDDDDD" />
        </DockPanel>

        <!-- 编辑区 -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">

            <!-- DataContext 切换为 UnifiedStage -->
            <StackPanel x:Name="EditPanel" Margin="20"
                        DataContext="{Binding SelectedItem}">

                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.7" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="关卡编辑 (Stage)" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333" />

                <!-- 1. 基础信息 -->
                <Expander Header="⚙️ 基础配置 &amp; 邀请函" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="ID:" />
                                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Name:" />
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Floor (楼层):" />
                                <TextBox Text="{Binding FloorNum, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBlock Text="Chapter (章节):" Margin="0,10,0,0" />
                                <TextBox Text="{Binding Chapter, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!-- Story Type 使用组件 -->
                            <StackPanel Grid.Column="4">
                                <!-- 复用 ImageSelector -->
                                <components:ImageSelector
                                    LabelText="Story Type (图标):"
                                    SelectedImageName="{Binding StoryType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    ImageList="{Binding DataContext.Manager.Artworks, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Manager="{Binding DataContext.Manager, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    PreviewWidth="100" PreviewHeight="100" />
                            </StackPanel>
                        </Grid>

                        <Separator Margin="0,15,0,15" />

                        <!-- 💌 邀请函 -->
                        <GroupBox Header="💌 邀请函配置 (Invitation)" Margin="0,0,0,0">
                            <StackPanel Margin="5">
                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="模式:" VerticalAlignment="Center" Margin="0,0,10,0" />
                                    <!-- 修正引用：core:GlobalValues -->
                                    <ComboBox ItemsSource="{x:Static core:GlobalValues.InvitationCombines}"
                                              SelectedItem="{Binding InvitationCombine, UpdateSourceTrigger=PropertyChanged}"
                                              Width="150" HorizontalAlignment="Left" />
                                </DockPanel>

                                <!-- BookValue -->
                                <Grid Visibility="{Binding IsBookValueMode, Converter={StaticResource BoolToVis}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="书籍总价值 (Value):" />
                                        <TextBox Text="{Binding InvitationValue, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="书籍数量 (Num):" />
                                        <TextBox Text="{Binding InvitationNum, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>

                                <!-- BookRecipe -->
                                <Grid Visibility="{Binding IsBookRecipeMode, Converter={StaticResource BoolToVis}}">
                                    <StackPanel>
                                        <TextBlock Text="指定书籍 ID列表:" Margin="0,0,0,5" />
                                        <ListBox ItemsSource="{Binding InvitationBooks}" Height="80" BorderBrush="#CCC"
                                                 BorderThickness="1">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <DockPanel>
                                                        <Button Content="❌" DockPanel.Dock="Right"
                                                                Click="RemoveInvBook_Click" Padding="5,0" />
                                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                                    </DockPanel>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                        <DockPanel Margin="0,5,0,0">
                                            <Button DockPanel.Dock="Right" Content="添加" Click="AddInvBook_Click"
                                                    Padding="10,2" Margin="5,0,0,0" />
                                            <TextBox x:Name="NewBookIdBox" Text="90081001" ToolTip="输入 DropBook ID" />
                                        </DockPanel>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </Expander>

                <!-- 2. 波次管理 (Waves) -->
                <Expander Header="🌊 战斗波次 (Waves)" IsExpanded="True" Margin="0,0,0,15" BorderBrush="#DDD"
                          BorderThickness="1">
                    <StackPanel Margin="10">
                        <Button Content="➕ 添加新波次 (Wave)"
                                Command="{Binding DataContext.AddWaveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,10" />

                        <ItemsControl ItemsSource="{Binding Waves}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type wrappers:UnifiedWave}">
                                    <Border BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,10" Padding="10"
                                            CornerRadius="4" Background="#FAFAFA">
                                        <StackPanel>
                                            <DockPanel LastChildFill="False" Margin="0,0,0,5">
                                                <TextBlock Text="Wave Config" FontWeight="Bold"
                                                           VerticalAlignment="Center" />
                                                <Button Content="删除此波次" DockPanel.Dock="Right" Click="RemoveWave_Click"
                                                        Background="#FFE6E6" Padding="5,2" />
                                            </DockPanel>

                                            <Grid Margin="0,0,0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="Formation (阵型ID):" />
                                                    <TextBox
                                                        Text="{Binding FormationId, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Available Unit (人数):" />
                                                    <TextBox
                                                        Text="{Binding AvailableUnit, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                            </Grid>

                                            <StackPanel Margin="0,5,0,0">
                                                <TextBlock Text="Manager Script (舞台脚本):" FontWeight="Bold" />
                                                <TextBox
                                                    Text="{Binding ManagerScript, UpdateSourceTrigger=PropertyChanged}"
                                                    ToolTip="特殊战斗逻辑脚本 (如: KeterFinal_Angela)" />
                                            </StackPanel>

                                            <!-- 敌人列表 -->
                                            <TextBlock Text="Enemies (敌人单位):" FontWeight="Bold" Margin="0,5,0,2" />
                                            <ListBox ItemsSource="{Binding Units}" Height="100" BorderBrush="#CCC"
                                                     BorderThickness="1">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <DockPanel>
                                                            <Button Content="❌" DockPanel.Dock="Right" Padding="5,0"
                                                                    Margin="5,0,0,0" Foreground="Red"
                                                                    Click="RemoveUnit_Click"
                                                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Border}}" />

                                                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                                        </DockPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>

                                            <!-- 添加敌人 -->
                                            <DockPanel Margin="0,5,0,0">
                                                <Button DockPanel.Dock="Right" Content="添加" Click="AddUnit_Click"
                                                        Padding="10,2" Margin="5,0,0,0" />

                                                <ComboBox x:Name="EnemySelector"
                                                          ItemsSource="{Binding DataContext.Manager.EnemyRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          DisplayMemberPath="DisplayName"
                                                          SelectedValuePath="GlobalId" />
                                            </DockPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>