<UserControl x:Class="Synthesis.Feature.Book.BookEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"

             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:components="clr-namespace:Synthesis.Views.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:core="clr-namespace:Synthesis.Core"
             xmlns:book="clr-namespace:Synthesis.Feature.Book"

             x:Name="Root"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=book:BookEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="800" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 1. 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,0,1" Padding="8,4">

            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建书页" Command="{Binding CreateCommand}"
                        IsEnabled="{Binding Manager.BookRepo.HasData}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0" Padding="10,5" />

                <Button Content="🗑️ 删除书页" Command="{Binding DeleteCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="#FF5252" BorderBrush="#FF5252"
                        Padding="10,5" Margin="0,0,8,0" />
            </StackPanel>
        </Border>

        <!-- 2. 左侧列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top"
                     Margin="4" Padding="4"
                     TextChanged="SearchBox_TextChanged"
                     materialDesign:HintAssist.Hint="搜索 ID 或 名称..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" />

            <ListBox x:Name="BookList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.BookRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     BorderThickness="0,1,1,0"
                     BorderBrush="{DynamicResource MaterialDesignDivider}" />
        </DockPanel>

        <!-- 3. 右侧编辑区 -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">

            <StackPanel x:Name="EditPanel" Margin="20" DataContext="{Binding SelectedItem}">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.6" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="核心书页编辑 (Key Page)" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" />

                <!-- Section 1: 基础参数 -->
                <Expander Header="⚙️ 基础参数 (Basic)" IsExpanded="True" Margin="0,0,0,10">
                    <StackPanel Margin="0,10,0,0">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="ID" />

                            <TextBox Grid.Column="2" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="Name (名称)" />

                            <Button Grid.Column="3" Content="💬 生成对话模板" Click="GenDialog_Click"
                                    Margin="10,0,0,0" Padding="10,4"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Foreground="Cyan" BorderBrush="Cyan" />
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding Episode, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="Episode (接待ID)" />

                            <ComboBox Grid.Column="2" ItemsSource="{x:Static core:GlobalValues.Rarities}"
                                      SelectedItem="{Binding Rarity, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="Rarity" />

                            <ComboBox Grid.Column="4" ItemsSource="{x:Static core:GlobalValues.BookChapters}"
                                      SelectedItem="{Binding Chapter, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="Chapter (等级)"
                                      ToolTip="1:传闻...7:杂质" />

                            <ComboBox Grid.Column="6" ItemsSource="{x:Static core:GlobalValues.RangeTypes}"
                                      SelectedItem="{Binding RangeType, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="RangeType" />
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" /><ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <components:ImageSelector LabelText="Book Icon"
                                                          SelectedImageName="{Binding BookIcon, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                          ImageList="{Binding DataContext.Manager.Artworks, ElementName=Root}"
                                                          Manager="{Binding DataContext.Manager, ElementName=Root}"
                                                          PreviewWidth="80" PreviewHeight="80" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                                <ComboBox ItemsSource="{x:Static core:GlobalValues.SkinTypes}"
                                          SelectedItem="{Binding SkinType, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                          materialDesign:HintAssist.Hint="Skin Type (皮肤类型)"
                                          Margin="0,0,0,10" />

                                <ComboBox IsEditable="True"
                                          Text="{Binding CharacterSkin, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                          materialDesign:HintAssist.Hint="Character Skin (模型)">
                                    <ComboBox.Items>
                                        <system:String>Default</system:String>
                                        <system:String>Liwei</system:String>
                                        <system:String>Roland</system:String>
                                        <system:String>Angela</system:String>
                                    </ComboBox.Items>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- Section 2: 战斗数值 -->
                <Expander Header="📊 战斗数值 (Stats)" IsExpanded="True" Margin="0,0,0,10">
                    <Grid Margin="0,15,0,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="15" /><ColumnDefinition Width="*" />
                            <ColumnDefinition Width="15" /><ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding HP, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="HP (体力)" Margin="0,0,0,15" />

                        <TextBox Grid.Row="0" Grid.Column="2"
                                 Text="{Binding Break, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="Break (混乱)" Margin="0,0,0,15" />

                        <StackPanel Grid.Row="0" Grid.Column="4">
                            <TextBlock Text="Speed (速度)" FontSize="10" Opacity="0.7" />
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding SpeedMin, UpdateSourceTrigger=PropertyChanged}" Width="40"
                                         Style="{StaticResource MaterialDesignTextBox}"
                                         HorizontalContentAlignment="Center" />
                                <TextBlock Text=" - " VerticalAlignment="Center" Margin="5,0" />
                                <TextBox Text="{Binding Speed, UpdateSourceTrigger=PropertyChanged}" Width="40"
                                         Style="{StaticResource MaterialDesignTextBox}"
                                         HorizontalContentAlignment="Center" />
                            </StackPanel>
                        </StackPanel>

                        <TextBox Grid.Row="1" Grid.Column="0"
                                 Text="{Binding StartPlayPoint, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="Start Light (初始光)" />

                        <TextBox Grid.Row="1" Grid.Column="2"
                                 Text="{Binding MaxPlayPoint, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="Max Light (最大光)" />
                    </Grid>
                </Expander>

                <!-- Section 3: 抗性 -->
                <Expander Header="🛡️ 抗性 (Resistances)" IsExpanded="True" Margin="0,0,0,10">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="1" Text="🔪 斩击" HorizontalAlignment="Center" Opacity="0.7" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="📌 穿刺" HorizontalAlignment="Center" Opacity="0.7" />
                        <TextBlock Grid.Row="0" Grid.Column="3" Text="🔨 打击" HorizontalAlignment="Center" Opacity="0.7" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="物理" VerticalAlignment="Center" FontWeight="Bold"
                                   Margin="0,10,0,0" />

                        <ComboBox Grid.Row="1" Grid.Column="1" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding SResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                        <ComboBox Grid.Row="1" Grid.Column="2" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding PResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                        <ComboBox Grid.Row="1" Grid.Column="3" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding HResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="混乱" VerticalAlignment="Center" FontWeight="Bold"
                                   Margin="0,10,0,0" />

                        <ComboBox Grid.Row="2" Grid.Column="1" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding SBResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                        <ComboBox Grid.Row="2" Grid.Column="2" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding PBResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                        <ComboBox Grid.Row="2" Grid.Column="3" Margin="4"
                                  ItemsSource="{x:Static core:GlobalValues.Resistances}"
                                  SelectedItem="{Binding HBResist, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                    </Grid>
                </Expander>

                <!-- Section 4: 核心被动 -->
                <Expander Header="⚡ 核心被动 (Passives)" IsExpanded="True" Margin="0,0,0,10">
                    <StackPanel Margin="0,10,0,0">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- 使用 ElementName=Root -->
                            <ComboBox Grid.Column="0" x:Name="PassiveSelector"
                                      IsEditable="True" StaysOpenOnEdit="True" IsTextSearchEnabled="False"
                                      ItemsSource="{Binding DataContext.Manager.PassiveRepo.Items, ElementName=Root}"
                                      SelectedItem="{Binding DataContext.SelectedPassiveToAdd, ElementName=Root}"
                                      DisplayMemberPath="DisplayName"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="选择被动能力..." />

                            <Button Grid.Column="1" Content="➕ 添加"
                                    Command="{Binding DataContext.AddPassiveCommand, ElementName=Root}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="10,0,0,0" VerticalAlignment="Bottom" />
                        </Grid>

                        <!-- 修改后的被动列表 -->
                        <ListBox ItemsSource="{Binding Passives}"
                                 MaxHeight="300"
                                 PreviewMouseWheel="IgnoreMouseWheel"
                                 Background="Transparent"
                                 Style="{StaticResource MaterialDesignOutlinedListBox}"
                                 Padding="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                    <Setter Property="Padding" Value="10,5" />
                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" />

                                        <!-- 删除按钮 -->
                                        <Button Grid.Column="1"
                                                Command="{Binding DataContext.RemovePassiveCommand, ElementName=Root}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignToolButton}"
                                                Foreground="#FF5252" Height="24" Width="24" Padding="0">
                                            <materialDesign:PackIcon Kind="Close" />
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Expander>

                <!-- Section 5: 专属书页 (双列表风格) -->
                <Expander Header="🃏 专属书页 (Only Cards)" IsExpanded="True" Margin="0,0,0,15">
                    <materialDesign:Card Margin="0,10,0,0" Background="#1AFFFFFF" UniformCornerRadius="8"
                                         materialDesign:ElevationAssist.Elevation="Dp0">
                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1"
                                CornerRadius="8" Padding="15">

                            <Grid Height="350">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- 左侧：已绑定 -->
                                <DockPanel Grid.Column="0">
                                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                        <materialDesign:PackIcon Kind="LinkVariant" Foreground="#FFCA28"
                                                                 VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <TextBlock Text="已绑定 (Bound)" FontWeight="Bold" Foreground="#FFCA28"
                                                   FontSize="14" />
                                    </StackPanel>

                                    <Border BorderBrush="#FFCA28" BorderThickness="1" CornerRadius="4"
                                            Background="#121212">
                                        <ListBox ItemsSource="{Binding OnlyCards}"
                                                 Background="Transparent"
                                                 Padding="0"
                                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="12,8" Background="Transparent" Cursor="Hand"
                                                            ToolTip="双击移除" BorderBrush="#333" BorderThickness="0,0,0,1">
                                                        <Border.InputBindings>
                                                            <MouseBinding Gesture="LeftDoubleClick"
                                                                          Command="{Binding DataContext.RemoveOnlyCardCommand, ElementName=Root}"
                                                                          CommandParameter="{Binding}" />
                                                        </Border.InputBindings>
                                                        <Grid>
                                                            <TextBlock Text="{Binding}" VerticalAlignment="Center"
                                                                       FontWeight="Medium"
                                                                       Foreground="{DynamicResource MaterialDesignBody}" />

                                                            <materialDesign:PackIcon Kind="ArrowRightBold"
                                                                    HorizontalAlignment="Right"
                                                                    Foreground="#FFCA28" Opacity="0.5" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </DockPanel>

                                <!-- 中间：指示 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                            Opacity="0.7">
                                    <materialDesign:PackIcon Kind="SwapHorizontalCircle" Width="40" Height="40"
                                                             Foreground="Gray" Margin="0,0,0,5" />
                                    <TextBlock Text="双击移动" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                                </StackPanel>

                                <!-- 右侧：卡牌库 -->
                                <DockPanel Grid.Column="2">
                                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                        <materialDesign:PackIcon Kind="Library" Foreground="Cyan"
                                                                 VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <TextBlock Text="卡牌库 (Library)" FontWeight="Bold" Foreground="Cyan"
                                                   FontSize="14" />
                                    </StackPanel>

                                    <Border BorderBrush="Cyan" BorderThickness="1" CornerRadius="4"
                                            Background="#121212">
                                        <ListBox
                                            ItemsSource="{Binding DataContext.Manager.CardRepo.Items, ElementName=Root}"
                                            Background="Transparent"
                                            Padding="0"
                                            ScrollViewer.VerticalScrollBarVisibility="Auto">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="12,8" Background="Transparent" Cursor="Hand"
                                                            ToolTip="双击绑定" BorderBrush="#333" BorderThickness="0,0,0,1">
                                                        <Border.InputBindings>
                                                            <MouseBinding Gesture="LeftDoubleClick"
                                                                          Command="{Binding DataContext.AddOnlyCardCommand, ElementName=Root}"
                                                                          CommandParameter="{Binding}" />
                                                        </Border.InputBindings>
                                                        <Grid>
                                                            <materialDesign:PackIcon Kind="ArrowLeftBold"
                                                                    HorizontalAlignment="Left"
                                                                    Foreground="Cyan" Opacity="0.5" />

                                                            <TextBlock Text="{Binding DisplayName}" Margin="24,0,0,0"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{DynamicResource MaterialDesignBody}" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </DockPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>
                </Expander>

                <!-- Section 6: 书籍故事 -->
                <Expander Header="📖 书籍故事 (Story)" IsExpanded="True" Margin="0,0,0,10">
                    <StackPanel Margin="0,10,0,0">
                        <TextBox Text="{Binding BookStory, UpdateSourceTrigger=PropertyChanged}"
                                 Height="150"
                                 PreviewMouseWheel="IgnoreMouseWheel"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="输入故事文本..."
                                 Padding="8" />
                    </StackPanel>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>