<UserControl x:Class="Synthesis.Feature.DropBook.DropBookEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"

             xmlns:components="clr-namespace:Synthesis.Views.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:wrappers="clr-namespace:Synthesis.Feature.DropBook"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=wrappers:DropBookEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="800" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 1. 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,0,1" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建书籍" Command="{Binding CreateCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,10,0" Padding="12,6" />
                <Button Content="🗑️ 删除书籍" Command="{Binding DeleteCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="#FF5252" BorderBrush="#FF5252"
                        Padding="12,6" Margin="0,0,10,0" />
            </StackPanel>
        </Border>

        <!-- 2. 左侧列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top"
                     Margin="4" Padding="4"
                     TextChanged="SearchBox_TextChanged"
                     materialDesign:HintAssist.Hint="搜索 ID 或 名称..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" />

            <ListBox x:Name="BookList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.DropBookRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="BookList_SelectionChanged"
                     BorderThickness="0,1,1,0"
                     BorderBrush="{DynamicResource MaterialDesignDivider}" />
        </DockPanel>

        <!-- 3. 右侧编辑区 -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EditPanel" Margin="30" DataContext="{Binding SelectedItem}">

                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.6" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="书籍与掉落编辑 (DropBook)" FontSize="22" FontWeight="Bold" Margin="0,0,0,25" />

                <!-- Section 1: 基础参数 -->
                <Expander Header="⚙️ 基础参数 (Basic)" IsExpanded="True" Margin="0,0,0,15">
                    <StackPanel Margin="0,15,0,0">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="ID" />

                            <TextBox Grid.Column="2" Text="{Binding TextId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="TextId (Name ID)" />
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" /><ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding Chapter, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Chapter (等级/章节)"
                                         Margin="0,0,0,15" />

                                <TextBox Text="{Binding LocalizedName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Localized Name (本地化名称)" />
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <components:ImageSelector
                                    LabelText="Book Icon"
                                    SelectedImageName="{Binding BookIcon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    ImageList="{Binding DataContext.Manager.Artworks, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Manager="{Binding DataContext.Manager, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    PreviewWidth="80" PreviewHeight="80" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- Section 2: 掉落核心书页 -->
                <Expander Header="🎁 掉落核心书页 (Equip Drops)" IsExpanded="True" Margin="0,0,0,15">
                    <StackPanel Margin="0,15,0,0">
                        <Button Content="➕ 添加核心书页掉落"
                                Command="{Binding DataContext.AddDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                HorizontalAlignment="Left" Margin="0,0,0,15" />

                        <ItemsControl ItemsSource="{Binding DropItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type wrappers:UnifiedDropItem}">
                                    <materialDesign:Card Margin="0,0,0,8" Padding="10"
                                                         Background="{DynamicResource MaterialDesignPaper}"
                                                         UniformCornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="核心书页:" VerticalAlignment="Center" Opacity="0.7"
                                                       Margin="0,0,10,0" />

                                            <!-- 书页选择器 -->
                                            <ComboBox Grid.Column="1"
                                                      ItemsSource="{Binding DataContext.Manager.BookRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedValue="{Binding ItemId, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="GlobalId"
                                                      DisplayMemberPath="DisplayName"
                                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                                      materialDesign:HintAssist.Hint="选择书页..."
                                                      IsEditable="True" />

                                            <Button Grid.Column="2" Content="{materialDesign:PackIcon Kind=Close}"
                                                    Command="{Binding DataContext.RemoveDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource MaterialDesignToolButton}"
                                                    Foreground="#FF5252" Height="24" Width="24" Margin="10,0,0,0" />
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Expander>

                <!-- Section 3: 战斗书页掉落 -->
                <Expander Header="🃏 掉落战斗书页 (Card Drops)" IsExpanded="True" Margin="0,0,0,15">
                    <StackPanel Margin="0,15,0,0">

                        <!-- 按钮：创建表 -->
                        <Button Content="为本书籍创建卡牌掉落表"
                                Command="{Binding DataContext.CreateCardDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                HorizontalAlignment="Left">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding DataContext.CurrentCardDrop, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- 表格区域 -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding DataContext.CurrentCardDrop, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- 添加栏 -->
                            <DockPanel Grid.Row="0" Margin="0,0,0,15">
                                <Button DockPanel.Dock="Right" Content="添加"
                                        Command="{Binding DataContext.AddCardDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Padding="10,4" Margin="10,0,0,0" Height="30" />

                                <ComboBox x:Name="CardSelector"
                                          ItemsSource="{Binding DataContext.Manager.CardRepo.Items, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          SelectedItem="{Binding DataContext.SelectedCardToAdd, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          DisplayMemberPath="DisplayName"
                                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                          materialDesign:HintAssist.Hint="搜索并添加战斗书页..."
                                          IsEditable="True" />
                            </DockPanel>

                            <!-- 列表 (Chips 风格) -->
                            <materialDesign:Card Grid.Row="1" Background="#1AFFFFFF" Padding="10"
                                                 UniformCornerRadius="4" materialDesign:ElevationAssist.Elevation="Dp0">
                                <ItemsControl
                                    ItemsSource="{Binding DataContext.CurrentCardDrop.CardIds, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <materialDesign:Chip Content="{Binding}"
                                                                 IsDeletable="True"
                                                                 DeleteCommand="{Binding DataContext.RemoveCardDropCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 DeleteCommandParameter="{Binding}"
                                                                 Margin="0,0,8,8" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </materialDesign:Card>
                        </Grid>
                    </StackPanel>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>