<UserControl x:Class="Synthesis.Feature.Enemy.EnemyEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:enemy="clr-namespace:Synthesis.Feature.Enemy"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=enemy:EnemyEditorViewModel, IsDesignTimeCreatable=False}"

             x:Name="Root"
             d:DesignHeight="800" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 1. 工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,0,1" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ 新建敌人" Command="{Binding CreateCommand}"
                        IsEnabled="{Binding Manager.EnemyRepo.HasData}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,10,0" Padding="12,6" />
                <Button Content="🗑️ 删除敌人" Command="{Binding DeleteCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="#FF5252" BorderBrush="#FF5252"
                        Padding="12,6" Margin="0,0,10,0" />
            </StackPanel>
        </Border>

        <!-- 2. 左侧列表 -->
        <DockPanel Grid.Row="1" Grid.Column="0">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top"
                     Margin="4" Padding="4"
                     TextChanged="SearchBox_TextChanged"
                     materialDesign:HintAssist.Hint="搜索 ID 或 名称..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" />

            <ListBox x:Name="EnemyList"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ItemsSource="{Binding Manager.EnemyRepo.Items}"
                     SelectedItem="{Binding SelectedItem}"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="EnemyList_SelectionChanged"
                     BorderThickness="0,1,1,0"
                     BorderBrush="{DynamicResource MaterialDesignDivider}" />
        </DockPanel>

        <!-- 3. 右侧编辑区 -->
        <ScrollViewer x:Name="ScrollViewer" Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">

            <!-- 注意：DataContext 依然是 SelectedItem -->
            <StackPanel x:Name="EditPanel" Margin="30" DataContext="{Binding SelectedItem}">

                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsVanilla}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Opacity" Value="0.6" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="敌人单位编辑 (Enemy Unit)" FontSize="22" FontWeight="Bold" Margin="0,0,0,25" />

                <!-- Section 1: 基础参数 -->
                <Expander Header="⚙️ 基础参数 (Basic)" IsExpanded="True" Margin="0,0,0,15">
                    <StackPanel Margin="0,15,0,0">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="ID" />

                            <TextBox Grid.Column="2" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="Name (名称)" />
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding NameId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="NameID (翻译ID)"
                                     ToolTip="默认与ID相同" />

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Height Range (身高范围)" FontSize="10" Opacity="0.6" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding MinHeight, UpdateSourceTrigger=PropertyChanged}" Width="60"
                                             Style="{StaticResource MaterialDesignTextBox}"
                                             HorizontalContentAlignment="Center" />
                                    <TextBlock Text=" - " VerticalAlignment="Center" Margin="5,0" />
                                    <TextBox Text="{Binding MaxHeight, UpdateSourceTrigger=PropertyChanged}" Width="60"
                                             Style="{StaticResource MaterialDesignTextBox}"
                                             HorizontalContentAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <!-- 核心书页绑定 (保持原始逻辑：SelectedValue + SelectedValuePath) -->
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- 使用 ElementName=Root 确保能找到 BookRepo -->
                            <ComboBox Grid.Column="0"
                                      SelectedValue="{Binding BookId, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="GlobalId"
                                      DisplayMemberPath="DisplayName"
                                      ItemsSource="{Binding DataContext.Manager.BookRepo.Items, ElementName=Root}"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="Equip Page (核心书页)"
                                      IsEditable="True" />

                            <!-- 清除按钮：保持 Click 事件 -->
                            <Button Grid.Column="1" Content="清除" Click="ClearBook_Click"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Foreground="Gray" BorderBrush="Gray"
                                    Margin="10,0,0,0" Padding="10,4" VerticalAlignment="Bottom" />
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- Section 2: 行为与掉落 -->
                <Expander Header="🎁 行为与掉落 (Drops)" IsExpanded="True" Margin="0,0,0,15">
                    <StackPanel Margin="0,15,0,0">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0" Content="死亡时撤退 (Retreat)"
                                      IsChecked="{Binding Retreat, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignSecondaryCheckBox}" />

                            <TextBox Grid.Column="2" Text="{Binding DropLevel, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="Drop Level (掉落等级)" />
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Books (书籍掉落)" VerticalAlignment="Bottom" FontWeight="Bold" Opacity="0.6" />
                            <!-- 这里的 Command 也改用 ElementName=Root -->
                            <Button HorizontalAlignment="Right" Content="➕ 添加书籍"
                                    Command="{Binding DataContext.AddDropCommand, ElementName=Root}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Padding="10,4" Height="28" />
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Drops}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card Margin="0,0,0,8" Padding="8"
                                                         Background="{DynamicResource MaterialDesignPaper}"
                                                         UniformCornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="10" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBox Grid.Column="0"
                                                     Text="{Binding Prob, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                     materialDesign:HintAssist.Hint="Prob"
                                                     Width="70" VerticalAlignment="Center" Padding="4" />

                                            <ComboBox Grid.Column="2"
                                                      ItemsSource="{Binding DataContext.Manager.DropBookRepo.Items, ElementName=Root}"
                                                      SelectedValue="{Binding BookId, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="GlobalId"
                                                      DisplayMemberPath="DisplayName"
                                                      IsEditable="True"
                                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                                      materialDesign:HintAssist.Hint="选择掉落书籍..." />

                                            <Button Grid.Column="3" Content="{materialDesign:PackIcon Kind=Close}"
                                                    Command="{Binding DataContext.RemoveDropCommand, ElementName=Root}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource MaterialDesignToolButton}"
                                                    Foreground="#FF5252" Height="30" Width="30" Margin="10,0,0,0" />
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Expander>

                <!-- 3. 卡组构建 -->
                <Expander Header="🃏 卡组构建 (Deck)" IsExpanded="True" Margin="0,0,0,15">

                    <!-- 外层容器：加一个 Card 或者带有边框的 Border，把整个构建区包起来 -->
                    <materialDesign:Card Margin="0,10,0,0" Background="#1AFFFFFF" UniformCornerRadius="8"
                                         materialDesign:ElevationAssist.Elevation="Dp0">
                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1"
                                CornerRadius="8" Padding="15">

                            <Grid Height="350">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- =========================== -->
                                <!-- 左侧：当前持有 (Deck) -->
                                <!-- =========================== -->
                                <DockPanel Grid.Column="0">
                                    <!-- 标题：强制使用亮橙色，确保看得清 -->
                                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                        <materialDesign:PackIcon Kind="CardsPlaying" Foreground="#FFCA28"
                                                                 VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <TextBlock Text="当前持有 (Deck)" FontWeight="Bold" Foreground="#FFCA28"
                                                   FontSize="14" />
                                    </StackPanel>

                                    <!-- 列表容器：加上边框和深色背景，形成“槽位”感 -->
                                    <Border BorderBrush="#FFCA28" BorderThickness="1" CornerRadius="4"
                                            Background="#121212">
                                        <ListBox x:Name="CurrentDeckList"
                                                 ItemsSource="{Binding DeckCardIds}"
                                                 Background="Transparent"
                                                 Padding="0"
                                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="12,8" Background="Transparent" Cursor="Hand"
                                                            ToolTip="双击移除" BorderBrush="#333" BorderThickness="0,0,0,1">
                                                        <Border.InputBindings>
                                                            <MouseBinding Gesture="LeftDoubleClick"
                                                                          Command="{Binding DataContext.RemoveCardFromDeckCommand, ElementName=Root}"
                                                                          CommandParameter="{Binding}" />
                                                        </Border.InputBindings>
                                                        <Grid>
                                                            <!-- 文字 -->
                                                            <TextBlock Text="{Binding}" VerticalAlignment="Center"
                                                                       FontWeight="Medium"
                                                                       Foreground="{DynamicResource MaterialDesignBody}" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </DockPanel>

                                <!-- =========================== -->
                                <!-- 中间：操作指示 -->
                                <!-- =========================== -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                            Opacity="0.7">
                                    <materialDesign:PackIcon Kind="SwapHorizontalCircle" Width="40" Height="40"
                                                             Foreground="Gray" Margin="0,0,0,5" />
                                    <TextBlock Text="双击移动" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                                    <TextBlock Text="Double Click" FontSize="10" Foreground="Gray"
                                               HorizontalAlignment="Center" />
                                </StackPanel>

                                <!-- =========================== -->
                                <!-- 右侧：卡牌库 (Library) -->
                                <!-- =========================== -->
                                <DockPanel Grid.Column="2">
                                    <!-- 标题：强制使用亮青色 -->
                                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                        <materialDesign:PackIcon Kind="Library" Foreground="Cyan"
                                                                 VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <TextBlock Text="卡牌库 (Library)" FontWeight="Bold" Foreground="Cyan"
                                                   FontSize="14" />
                                    </StackPanel>

                                    <!-- 列表容器：加上边框和深色背景 -->
                                    <Border BorderBrush="Cyan" BorderThickness="1" CornerRadius="4"
                                            Background="#121212">
                                        <ListBox x:Name="LibraryCardList"
                                                 ItemsSource="{Binding DataContext.Manager.CardRepo.Items, ElementName=Root}"
                                                 Background="Transparent"
                                                 Padding="0"
                                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="12,8" Background="Transparent" Cursor="Hand"
                                                            ToolTip="双击添加" BorderBrush="#333" BorderThickness="0,0,0,1">
                                                        <Border.InputBindings>
                                                            <MouseBinding Gesture="LeftDoubleClick"
                                                                          Command="{Binding DataContext.AddCardToDeckCommand, ElementName=Root}"
                                                                          CommandParameter="{Binding}" />
                                                        </Border.InputBindings>
                                                        <Grid>
                                                            <materialDesign:PackIcon Kind="ArrowLeftBold"
                                                                    HorizontalAlignment="Left"
                                                                    Foreground="Cyan" Opacity="0.5" />

                                                            <!-- 文字：强制白色/浅灰 -->
                                                            <TextBlock Text="{Binding DisplayName}" Margin="24,0,0,0"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{DynamicResource MaterialDesignBody}" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </DockPanel>
                            </Grid>
                        </Border>
                    </materialDesign:Card>
                </Expander>

                <TextBlock Height="50" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>