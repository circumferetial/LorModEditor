<windows:Window
  x:Class="Synthesis.Feature.MainWindow.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:prism="http://prismlibrary.com/"
  prism:ViewModelLocator.AutoWireViewModel="True"
  FrameworkElement.Height="800"
  FrameworkElement.Width="1200"
  Control.Background="{DynamicResource MaterialDesignPaper}"
  TextElement.Foreground="{DynamicResource MaterialDesignBody}"
  xmlns:windows="clr-namespace:System.Windows;assembly=PresentationFramework"
  Title="{Binding Title}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style
        x:Key="SideNavButtonStyle"
        TargetType="{x:Type RadioButton}">
        <Setter
          Property="Height"
          Value="50" />
        <Setter
          Property="Margin"
          Value="0,0,0,2" />
        <Setter
          Property="Background"
          Value="#00FFFFFF" />
        <Setter
          Property="BorderThickness"
          Value="0" />
        <Setter
          Property="Foreground"
          Value="#AAAAAA" />
        <Setter
          Property="FontSize"
          Value="20" />
        <Setter
          Property="FrameworkElement.Cursor"
          Value="Hand" />
        <Setter
          Property="Template">
          <Setter.Value>
            <ControlTemplate
              TargetType="{x:Type RadioButton}">
              <Border
                Background="{TemplateBinding Control.Background}"
                CornerRadius="0">
                <Grid>
                  <Border
                    x:Name="Indicator"
                    Width="4"
                    HorizontalAlignment="Left"
                    Background="#00FFFFFF" />
                  <ContentPresenter
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger
                  Property="UIElement.IsMouseOver"
                  Value="True">
                  <Setter
                    Property="Background"
                    Value="#333333" />
                  <Setter
                    Property="Foreground"
                    Value="#FFFFFF" />
                </Trigger>
                <Trigger
                  Property="ToggleButton.IsChecked"
                  Value="True">
                  <Setter
                    Property="Background"
                    Value="#252526" />
                  <Setter
                    Property="Foreground"
                    Value="#FFFFA500" />
                  <Setter
                    TargetName="Indicator"
                    Property="Border.Background"
                    Value="#FFFFA500" />
                  <Setter
                    Property="FontWeight"
                    Value="Bold" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition
        Width="60" />
      <ColumnDefinition
        Width="*" />
    </Grid.ColumnDefinitions>
    <Border
      Grid.Column="0"
      Background="#1E1E1E"
      BorderBrush="#333333"
      BorderThickness="0,0,1,0">
      <StackPanel
        Margin="0,10,0,0">
        <RadioButton
          CommandParameter="CardEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          IsChecked="True"
          ToolTip="战斗书页"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="🎴" />
        </RadioButton>
        <RadioButton
          CommandParameter="PassiveEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="被动能力"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="⚡" />
        </RadioButton>
        <RadioButton
          CommandParameter="BookEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="核心书页"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="📖" />
        </RadioButton>
        <RadioButton
          CommandParameter="EnemyEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="敌人单位"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="👹" />
        </RadioButton>
        <RadioButton
          CommandParameter="StageEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="关卡配置"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="🏰" />
        </RadioButton>
        <RadioButton
          CommandParameter="DropBookEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="书籍掉落"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="📚" />
        </RadioButton>
        <RadioButton
          CommandParameter="SkinEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="皮肤编辑"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="🎨" />
        </RadioButton>
        <Separator
          Background="#444"
          Margin="10,5" />
        <RadioButton
          CommandParameter="AbilityEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="能力描述"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="📝" />
        </RadioButton>
        <RadioButton
          CommandParameter="KeywordEditorView"
          Style="{StaticResource SideNavButtonStyle}"
          ToolTip="关键字"
          Command="{Binding NavigateCommand}">
          <TextBlock
            Text="🔤" />
        </RadioButton>
      </StackPanel>
    </Border>
    <RadioButton
      Grid.Column="0"
      VerticalAlignment="Bottom"
      Height="60"
      CommandParameter="SettingsView"
      Style="{StaticResource SideNavButtonStyle}"
      ToolTip="设置"
      Command="{Binding NavigateCommand}">
      <TextBlock
        Text="⚙️" />
    </RadioButton>
    <Grid
      Column="1">
      <Grid.RowDefinitions>
        <RowDefinition
          Height="Auto" />
        <RowDefinition
          Height="*" />
      </Grid.RowDefinitions>
      <Border
        Grid.Row="0"
        Background="{DynamicResource MaterialDesignPaper}"
        BorderBrush="#333333"
        BorderThickness="0,0,0,1"
        Padding="10">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition
              Width="Auto" />
            <ColumnDefinition
              Width="*" />
            <ColumnDefinition
              Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel
            Grid.Column="0"
            Orientation="Horizontal">
            <Button
              Content="📂 打开项目"
              Padding="15,6"
              Margin="0,0,10,0"
              Style="{StaticResource MaterialDesignRaisedButton}"
              Command="{Binding OpenProjectCommand}" />
            <Button
              Content="💾 保存所有"
              Padding="15,6"
              Margin="0,0,10,0"
              Style="{StaticResource MaterialDesignRaisedButton}"
              Command="{Binding SaveProjectCommand}" />
            <Button
              Content="📝 导出文档"
              Padding="15,6"
              Style="{StaticResource MaterialDesignOutlinedButton}"
              BorderBrush="#FF00FFFF"
              Foreground="#FF00FFFF"
              Command="{Binding ExportCommand}" />
          </StackPanel>
          <ContentControl
            Grid.Column="2"
            prism:RegionManager.RegionName="ToolbarRegion" />
        </Grid>
      </Border>
      <ContentControl
        Grid.Row="1"
        prism:RegionManager.RegionName="ContentRegion"
        Margin="0" />
    </Grid>
  </Grid>
</windows:Window>
