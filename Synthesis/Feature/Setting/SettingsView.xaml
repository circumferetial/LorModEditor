<controls:UserControl
  x:Class="Synthesis.Feature.Setting.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:prism="http://prismlibrary.com/"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:sys="clr-namespace:System;assembly=mscorlib"
  prism:ViewModelLocator.AutoWireViewModel="True"
  TextElement.Foreground="{DynamicResource MaterialDesignBody}"
  xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework">
  <Grid
    Background="{DynamicResource MaterialDesignPaper}">
    <ScrollViewer
      VerticalScrollBarVisibility="Auto">
      <StackPanel
        Margin="40"
        MaxWidth="800"
        HorizontalAlignment="Left"
        VerticalAlignment="Top">
        <TextBlock
          Text="⚙️ 编辑器设置 (Settings)"
          FontSize="28"
          FontWeight="Bold"
          Margin="0,0,0,30"
          Foreground="{DynamicResource MaterialDesignBody}" />
        <materialDesign:Card
          Control.Background="{DynamicResource MaterialDesignPaper}"
          UniformCornerRadius="8"
          Control.Padding="20"
          FrameworkElement.Margin="0,0,0,25"
          materialDesign:ElevationAssist.Elevation="Dp2">
          <StackPanel>
            <TextBlock
              Text="🌍 常规 (General)"
              Style="{StaticResource MaterialDesignHeadline6TextBlock}"
              Margin="0,0,0,15" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition
                  Width="Auto" />
                <ColumnDefinition
                  Width="*" />
              </Grid.ColumnDefinitions>
              <ComboBox
                Grid.Column="0"
                Width="200"
                Height="45"
                IsEditable="True"
                Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                materialDesign:HintAssist.Hint="Target Language (目标语言)"
                Text="{Binding Config.Language, UpdateSourceTrigger=PropertyChanged}">
                <ItemsControl.Items>
                  <sys:String>cn</sys:String>
                  <sys:String>en</sys:String>
                  <sys:String>kr</sys:String>
                  <sys:String>jp</sys:String>
                  <sys:String>tr</sys:String>
                </ItemsControl.Items>
              </ComboBox>
              <TextBlock
                Grid.Column="1"
                VerticalAlignment="Center"
                Margin="20,10,0,0"
                Text="* 决定读取 Localize 下的哪个文件夹 (cn/en/kr...)"
                FontSize="12"
                Opacity="0.6"
                TextWrapping="Wrap" />
            </Grid>
          </StackPanel>
        </materialDesign:Card>
        <materialDesign:Card
          Control.Background="{DynamicResource MaterialDesignPaper}"
          UniformCornerRadius="8"
          Control.Padding="20"
          FrameworkElement.Margin="0,0,0,25"
          materialDesign:ElevationAssist.Elevation="Dp2">
          <StackPanel>
            <TextBlock
              Text="📦 原版数据 (Vanilla Data)"
              Style="{StaticResource MaterialDesignHeadline6TextBlock}"
              Margin="0,0,0,15" />
            <CheckBox
              Content="加载原版游戏数据 (Load Vanilla Data)"
              Style="{StaticResource MaterialDesignSecondaryCheckBox}"
              FontSize="14"
              Margin="0,0,0,20"
              ToolTip="关闭后可加快加载速度..."
              IsChecked="{Binding Config.ShowVanillaData}" />
            <DockPanel
              LastChildFill="True">
              <Button
                DockPanel.Dock="Right"
                Content="📂 浏览..."
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Margin="10,0,0,0"
                Padding="15,4"
                VerticalAlignment="Bottom"
                Command="{Binding BrowseCommand}" />
              <TextBox
                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                materialDesign:HintAssist.Hint="BaseMod 路径 (LibraryOfRuina_Data\Managed\BaseMod)"
                VerticalContentAlignment="Center"
                Text="{Binding Config.BaseModPath, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
          </StackPanel>
        </materialDesign:Card>
        <StackPanel
          Orientation="Vertical"
          HorizontalAlignment="Right"
          Margin="0,10,0,0">
          <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <Button
              Content="💾 保存设置"
              Style="{StaticResource MaterialDesignRaisedButton}"
              Padding="25,6"
              Margin="0,0,15,0"
              Height="Auto"
              FontSize="14"
              FontWeight="Bold"
              Command="{Binding SaveCommand}" />
            <Button
              Content="🔄 重新加载项目"
              Style="{StaticResource MaterialDesignOutlinedButton}"
              Padding="20,6"
              Height="Auto"
              FontSize="14"
              FontWeight="Bold"
              Foreground="#00E5FF"
              BorderBrush="#00E5FF"
              ToolTip="应用新的语言或路径设置"
              Command="{Binding ReloadCommand}" />
          </StackPanel>
          <TextBlock
            Text="* 更改语言或路径后，请点击“重新加载项目”以生效。"
            FontSize="12"
            Opacity="0.5"
            Margin="0,10,5,0"
            HorizontalAlignment="Right" />
        </StackPanel>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</controls:UserControl>
