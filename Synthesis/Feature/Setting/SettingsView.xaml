<UserControl x:Class="Synthesis.Feature.Setting.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:setting="clr-namespace:Synthesis.Feature.Setting"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=setting:SettingsViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="600" d:DesignWidth="900"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Background="{DynamicResource MaterialDesignPaper}">

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40" MaxWidth="800" HorizontalAlignment="Left" VerticalAlignment="Top">

                <TextBlock Text="⚙️ 编辑器设置 (Settings)"
                           FontSize="28" FontWeight="Bold" Margin="0,0,0,30"
                           Foreground="{DynamicResource MaterialDesignBody}" />

                <!-- 1. 常规设置 -->
                <materialDesign:Card Background="{DynamicResource MaterialDesignPaper}"
                                     UniformCornerRadius="8" Padding="20" Margin="0,0,0,25"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="🌍 常规 (General)"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,15" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <ComboBox Grid.Column="0" Width="200" Height="45"
                                      IsEditable="True"
                                      Text="{Binding Config.Language, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                      materialDesign:HintAssist.Hint="Target Language (目标语言)">
                                <ComboBox.Items>
                                    <sys:String>cn</sys:String>
                                    <sys:String>en</sys:String>
                                    <sys:String>kr</sys:String>
                                    <sys:String>jp</sys:String>
                                    <sys:String>tr</sys:String>
                                </ComboBox.Items>
                            </ComboBox>

                            <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="20,10,0,0"
                                       Text="* 决定读取 Localize 下的哪个文件夹 (cn/en/kr...)"
                                       FontSize="12" Opacity="0.6" TextWrapping="Wrap" />
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 2. 原版数据设置 -->
                <materialDesign:Card Background="{DynamicResource MaterialDesignPaper}"
                                     UniformCornerRadius="8" Padding="20" Margin="0,0,0,25"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="📦 原版数据 (Vanilla Data)"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,15" />

                        <CheckBox Content="加载原版游戏数据 (Load Vanilla Data)"
                                  IsChecked="{Binding Config.LoadVanillaData}"
                                  Style="{StaticResource MaterialDesignSecondaryCheckBox}"
                                  FontSize="14" Margin="0,0,0,20"
                                  ToolTip="关闭后可加快加载速度..." />

                        <DockPanel LastChildFill="True">
                            <Button DockPanel.Dock="Right" Content="📂 浏览..."
                                    Command="{Binding BrowseCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="10,0,0,0" Padding="15,4" VerticalAlignment="Bottom" />

                            <TextBox Text="{Binding Config.BaseModPath, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="BaseMod 路径 (LibraryOfRuina_Data\Managed\BaseMod)"
                                     VerticalContentAlignment="Center" />
                        </DockPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 3. 操作区 -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- 
                           [修复1] 保存按钮 
                           Padding 改为 "25,6" (减小垂直内边距)，防止文字被切掉。
                           Height="Auto" 让它自动适应内容。
                        -->
                        <Button Command="{Binding SaveCommand}"
                                Content="💾 保存设置"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Padding="25,6" Margin="0,0,15,0"
                                Height="Auto"
                                FontSize="14" FontWeight="Bold" />

                        <!-- 
                           [修复2] 重新加载按钮
                           Foreground/BorderBrush 强制设为 #00E5FF (亮青色) 或 #FFCA28 (金黄色)。
                           Padding 改为 "20,6"。
                        -->
                        <Button Command="{Binding ReloadCommand}"
                                Content="🔄 重新加载项目"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Padding="20,6"
                                Height="Auto"
                                FontSize="14"
                                FontWeight="Bold"
                                Foreground="#00E5FF"
                                BorderBrush="#00E5FF"
                                ToolTip="应用新的语言或路径设置" />
                    </StackPanel>

                    <TextBlock Text="* 更改语言或路径后，请点击“重新加载项目”以生效。"
                               FontSize="12" Opacity="0.5" Margin="0,10,5,0" HorizontalAlignment="Right" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>