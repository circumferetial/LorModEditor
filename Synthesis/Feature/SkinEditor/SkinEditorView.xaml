<UserControl x:Class="Synthesis.Feature.SkinEditor.SkinEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:skinEditor="clr-namespace:Synthesis.Feature.SkinEditor"

             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=skinEditor:SkinEditorViewModel, IsDesignTimeCreatable=False}"

             d:DesignHeight="800" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" /> <!-- 左侧导航稍微加宽 -->
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" /> <!-- 右侧属性面板 -->
        </Grid.ColumnDefinitions>

        <!-- =================================================================== -->
        <!-- [左侧] 皮肤与动作管理 -->
        <!-- =================================================================== -->
        <Grid Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- 皮肤管理 -->
                <RowDefinition Height="*" />    <!-- 动作列表 -->
            </Grid.RowDefinitions>

            <!-- 1. 皮肤选择区 -->
            <StackPanel Grid.Row="0" Margin="10">
                <TextBlock Text="Skin (皮肤)" FontWeight="Bold" Margin="0,0,0,5"
                           Foreground="{DynamicResource PrimaryHueMidBrush}" />

                <!-- 皮肤下拉框 -->
                <ComboBox x:Name="SkinListBox"
                          ItemsSource="{Binding Manager.SkinRepo.Items}"
                          SelectedItem="{Binding SelectedItem}"
                          DisplayMemberPath="DisplayName"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          materialDesign:HintAssist.Hint="选择当前编辑的皮肤..."
                          Margin="0,0,0,10" />

                <!-- 皮肤操作按钮 -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="➕ 新建皮肤" Command="{Binding CreateCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,5,0" />
                    <Button Content="🗑 删除皮肤" Command="{Binding DeleteSkinCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="#FF5252" BorderBrush="#FF5252" />
                </StackPanel>

                <TextBlock Margin="0,10,0,0"
                           Text="{Binding PublishCompatibilityText}"
                           FontSize="11"
                           Opacity="0.75"
                           TextWrapping="Wrap" />

                <Separator Background="{DynamicResource MaterialDesignDivider}" Margin="0,15" />
            </StackPanel>

            <!-- 2. 动作列表 (Action List) -->
            <DockPanel Grid.Row="1" Margin="10,0,10,10">
                <!-- 列表头 -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Actions (动作序列)" VerticalAlignment="Center" FontWeight="Bold" />

                    <WrapPanel Grid.Row="1" Margin="0,8,0,0">
                        <ComboBox Width="120" Margin="0,0,5,5"
                                  ItemsSource="{Binding PresetActions}"
                                  SelectedItem="{Binding SelectedPresetAction, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                  materialDesign:HintAssist.Hint="枚举动作" />
                        <Button Content="➕ 枚举动作" Margin="0,0,5,5"
                                Command="{Binding AddPresetActionCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Padding="8,4" Height="28" FontSize="12" />
                        <Button Content="✍️ 自定义动作" Margin="0,0,5,5"
                                Command="{Binding AddCustomActionCommand}"
                                Visibility="Collapsed"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Padding="8,4" Height="28" FontSize="12" />
                        <Button Content="🗑 删除动作" Margin="0,0,5,5"
                                Command="{Binding DeleteActionCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Foreground="#FF5252" BorderBrush="#FF5252"
                                Padding="8,4" Height="28" FontSize="12" />
                    </WrapPanel>
                </Grid>

                <!-- 动作列表 -->
                <ListBox x:Name="ActionList"
                         ItemsSource="{Binding SelectedItem.Actions}"
                         SelectedItem="{Binding SelectedAction}"
                         SelectionChanged="ActionList_SelectionChanged"
                         Style="{StaticResource MaterialDesignOutlinedListBox}"
                         Background="Transparent"
                         Padding="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- 简单的选中高亮条 -->
                            <Border Padding="10,8" Background="Transparent"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <materialDesign:PackIcon Kind="MovieOpen" Opacity="0.5" VerticalAlignment="Center"
                                                             Margin="0,0,10,0" />
                                    <TextBlock Grid.Column="1" Text="{Binding ActionName}" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>

        <!-- =================================================================== -->
        <!-- [中间] 视口画布 (Viewport) -->
        <!-- =================================================================== -->
        <Border Grid.Column="1" Background="#1E1E1E" ClipToBounds="True">
            <Grid>
                <!-- 背景网格装饰 (可选) -->
                <Canvas>
                    <Canvas.Background>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,40,40" ViewportUnits="Absolute" Opacity="0.05">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing>
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup>
                                            <LineGeometry StartPoint="0,0" EndPoint="0,40" />
                                            <LineGeometry StartPoint="0,0" EndPoint="40,0" />
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                    <GeometryDrawing.Pen>
                                        <Pen Brush="White" Thickness="1" />
                                    </GeometryDrawing.Pen>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Canvas.Background>
                </Canvas>

                <!-- 核心交互 Canvas (保留原逻辑) -->
                <Canvas x:Name="ViewportCanvas" Background="Transparent"
                        MouseWheel="Viewport_MouseWheel"
                        MouseRightButtonDown="Viewport_MouseRightDown"
                        MouseRightButtonUp="Viewport_MouseRightUp"
                        MouseMove="Viewport_MouseMove">

                    <!-- 世界层 -->
                    <Canvas x:Name="WorldCanvas">
                        <Canvas.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="ViewScale" ScaleX="1" ScaleY="1" />
                                <TranslateTransform x:Name="ViewTranslate" X="400" Y="300" />
                            </TransformGroup>
                        </Canvas.RenderTransform>

                        <!-- 坐标轴 -->
                        <!-- X轴 (地面) -->
                        <Line X1="-10000" Y1="0" X2="10000" Y2="0" Stroke="#00E676" StrokeThickness="2" Opacity="0.8"
                              IsHitTestVisible="False" />
                        <!-- Y轴 (中心) -->
                        <Line X1="0" Y1="-10000" X2="0" Y2="10000" Stroke="#00E676" StrokeThickness="1" Opacity="0.4"
                              IsHitTestVisible="False"
                              StrokeDashArray="4 4" />

                        <!-- 身体图片 -->
                        <!-- 增加 DropShadow 稍微让图片浮起来一点 -->
                        <Image x:Name="BodyImage"
                               Stretch="Fill"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"
                               MouseLeftButtonDown="Body_MouseDown"
                               MouseLeftButtonUp="Body_MouseUp"
                               Cursor="SizeAll">
                            <Image.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.2" />
                            </Image.Effect>
                        </Image>

                        <!-- 头部锚点 -->
                        <!-- 优化视觉：红点加白边，更容易看清 -->
                        <Grid x:Name="HeadPoint" Width="14" Height="14"
                              MouseLeftButtonDown="Head_MouseDown"
                              MouseLeftButtonUp="Head_MouseUp"
                              Cursor="Hand"
                              Visibility="{Binding SelectedAction.HeadEnable, Converter={StaticResource BoolToVis}}">
                            <!-- 这里的 Canvas.Left/Top 会在代码中被修改，注意 UserControl 的 DataContext 可能是 ViewModel，但 Canvas 里的位置是绝对控制的 -->
                            <Ellipse Fill="#FF5252" Stroke="White" StrokeThickness="2">
                                <Ellipse.Effect>
                                    <DropShadowEffect ShadowDepth="0" Color="Red" BlurRadius="8" />
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                    </Canvas>
                </Canvas>

                <!-- 悬浮指南 -->
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" Opacity="0.6"
                            IsHitTestVisible="False">
                    <TextBlock Text="🖱️ 右键拖拽移动视角" Foreground="White" FontSize="11" HorizontalAlignment="Right" />
                    <TextBlock Text="🖱️ 滚轮缩放画布" Foreground="White" FontSize="11" HorizontalAlignment="Right" />
                    <TextBlock Text="🖱️ 左键拖动图片/红点" Foreground="White" FontSize="11" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- =================================================================== -->
        <!-- [右侧] 属性面板 -->
        <!-- =================================================================== -->
        <Border Grid.Column="2" Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20" DataContext="{Binding SelectedAction}"
                            d:DataContext="{d:DesignInstance Type=skinEditor:UnifiedSkinAction}">

                    <!-- 只有选中动作才显示内容 -->
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Hidden" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="Action Properties" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

                    <!-- 动作名称 -->
                    <!-- ... (在 ActionName 下方) ... -->

                    <!-- Direction 设置 (面部朝向) -->


                    <!-- 图片操作区域 (合并导入和镜像按钮) -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /> <!-- 导入按钮占大头 -->
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" /> <!-- 镜像按钮 -->
                        </Grid.ColumnDefinitions>

                        <!-- 1. 导入按钮 (原有) -->
                        <Button Grid.Column="0"
                                Command="{Binding DataContext.ImportImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Height="50"
                                BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                                Foreground="{DynamicResource PrimaryHueMidBrush}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ImagePlus" Margin="0,0,10,0" />
                                <TextBlock Text="导入图片" FontWeight="Bold" />
                            </StackPanel>
                        </Button>

                        <!-- 2. 【新增】镜像翻转按钮 -->
                        <Button Grid.Column="2"
                                Command="{Binding DataContext.MirrorImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Height="50" Width="50"
                                ToolTip="水平镜像翻转图片 (Flip Horizontal)"
                                BorderBrush="OrangeRed"
                                Foreground="OrangeRed">
                            <materialDesign:PackIcon Kind="FlipHorizontal" Width="24" Height="24" />
                        </Button>
                    </Grid>

                    <!-- ... (接下面的 HD/Extended 模式开关) ... -->

                    <!-- HD 模式开关 -->
                    <materialDesign:Card Background="#1AFFFFFF" Padding="15" UniformCornerRadius="8" Margin="0,0,0,20"
                                         materialDesign:ElevationAssist.Elevation="Dp0">
                        <StackPanel>
                            <DockPanel LastChildFill="False">
                                <TextBlock Text="✨ Extended 模式" FontWeight="Bold" VerticalAlignment="Center" />

                                <!-- ❌ 错误写法: {Binding SelectedItem.IsExtended} -->
                                <!-- ✅ 修正写法: 使用 RelativeSource 找 ViewModel -->
                                <ToggleButton DockPanel.Dock="Right"
                                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                              IsChecked="{Binding DataContext.SelectedItem.IsExtended, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              ToolTip="开启 ModInfo Extended='true'" />
                            </DockPanel>
                            <TextBlock Text="ON: 启用高清/特效/自定义尺寸&#x0a;OFF: 强制原版兼容 (512px, SD)"
                                       FontSize="10" Opacity="0.5" Margin="0,5,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </materialDesign:Card>
                    <Expander Header="📐 扩展参数 (Size/Quality)" IsExpanded="True" Margin="0,0,0,10"
                              IsEnabled="{Binding DataContext.SelectedItem.IsExtended, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <StackPanel Margin="0,10,0,0">
                            <!-- 提示信息 -->
                            <TextBlock Text="⚠️ 修改此处会影响单位比例与头部坐标换算" Foreground="Orange" FontSize="11" Margin="0,0,0,10" />

                            <!-- 尺寸 Size -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding SizeX, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Size X (宽)" />
                                <TextBox Grid.Column="2"
                                         Text="{Binding SizeY, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Size Y (高)" />
                            </Grid>

                            <!-- 质量 Quality -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Quality, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Quality (PPU缩放)" />

                                <!-- 快速预设按钮 -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                                    <Button Content="SD(50)"
                                            Command="{Binding DataContext.SetQualityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="50"
                                            Style="{StaticResource MaterialDesignFlatButton}" Padding="4,0" Height="24"
                                            FontSize="10" Margin="5,0,0,0" />

                                    <Button Content="HD(100)"
                                            Command="{Binding DataContext.SetQualityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="100"
                                            Style="{StaticResource MaterialDesignFlatButton}" Padding="4,0" Height="24"
                                            FontSize="10" />
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="* 50 = 原版2倍缩放 (SD)&#x0a;* 100 = 1倍无缩放 (HD)&#x0a;* Quality 不影响身体 Pivot 对地面基准"
                                       FontSize="10" Opacity="0.5" Margin="0,5,0,0" />
                        </StackPanel>
                    </Expander>
                    <!-- Pivot (身体位置) -->
                    <Expander Header="📍 身体中心 (Pivot)" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <TextBox Text="{Binding PivotX, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Pivot X (左右)" />

                                <TextBox Grid.Column="2" Text="{Binding PivotY, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                         materialDesign:HintAssist.Hint="Pivot Y (上下)" />
                            </Grid>
                            <TextBlock Text="* 绿线为地面，请调整 Y 值使脚底对齐绿线&#x0a;* Pivot 与 Quality 无关"
                                       FontSize="10" Opacity="0.5" Margin="0,5,0,0"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Expander>

                    <!-- Head (头部挂载) -->
                    <Expander Header="🙆 头部挂载 (Head)" IsExpanded="True">
                        <StackPanel Margin="0,10,0,0">
                            <!-- 开关 -->
                            <CheckBox Content="启用头部挂载点"
                                      IsChecked="{Binding HeadEnable}"
                                      Style="{StaticResource MaterialDesignSecondaryCheckBox}"
                                      Margin="0,0,0,15" />

                            <!-- 参数区 (显隐) -->
                            <StackPanel Visibility="{Binding HeadEnable, Converter={StaticResource BoolToVis}}">
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="15" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Text="{Binding HeadX, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                             materialDesign:HintAssist.Hint="Head X" />

                                    <TextBox Grid.Column="2"
                                             Text="{Binding HeadY, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                             materialDesign:HintAssist.Hint="Head Y" />
                                </Grid>

                                 <TextBox Text="{Binding HeadRotation, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                          materialDesign:HintAssist.Hint="Rotation (角度)" />

                                 <TextBlock Text="* 拖拽画布中的红点可直接调整位置" FontSize="10" Opacity="0.5" Margin="0,5,0,0" />
                                <TextBlock Text="* Face 回退提示：Fire/Aim/Special/S1-S5/Slash2 在部分场景会回退到 Slash；Penetrate2 回退到 Penetrate；Hit2 回退到 Hit"
                                           FontSize="10" Opacity="0.5" Margin="0,3,0,6" TextWrapping="Wrap" />
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:PackIcon Kind="FaceManProfile" VerticalAlignment="Center"
                                                             Margin="0,0,10,0" Opacity="0.5" />

                                    <!-- Direction 下拉框 -->
                                    <ComboBox Grid.Column="1"
                                              Text="{Binding Direction, UpdateSourceTrigger=PropertyChanged}"
                                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                              materialDesign:HintAssist.Hint="面部朝向 (Direction)">
                                        <ComboBoxItem Content="Front" ToolTip="正脸 (默认)" />
                                        <ComboBoxItem Content="Side" ToolTip="侧脸 (战斗姿态常用)" />
                                    </ComboBox>
                                </Grid>
                            </StackPanel>


                        </StackPanel>
                    </Expander>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
